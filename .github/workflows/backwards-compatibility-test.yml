name: Backwards Compatibility Test
description: 'Test backwards compatibility with different Nx versions and Node versions. Node support per Nx major: https://nx.dev/docs/technologies/node/introduction'

on: workflow_call

jobs:
  backwards-compatibility-test:
    strategy:
      matrix:
        include:
          # '' = current workspace version (23.x) — all Node versions from Nx docs
          - nx-version: '' # 23.x
            node-version: 26
          - nx-version: ''
            node-version: 24
          - nx-version: ''
            node-version: 22

          # previous = npm dist-tag (22.x) — all Node versions from Nx docs
          - nx-version: 'previous' # 22.x
            node-version: 26
          - nx-version: 'previous'
            node-version: 24
          - nx-version: 'previous'
            node-version: 22
          - nx-version: 'previous'
            node-version: 20

          # pinned (21.x) — all Node versions from Nx docs
          - nx-version: '21.6.11' # 21.x
            node-version: 24
          - nx-version: '21.6.11'
            node-version: 22
          - nx-version: '21.6.11'
            node-version: 20

          # pinned (20.x) — all Node versions from Nx docs
          - nx-version: '20.8.4' # 20.x
            node-version: 22
          - nx-version: '20.8.4'
            node-version: 20
          - nx-version: '20.8.4'
            node-version: 18

          # minimum supported (19.x) — latest installable 19.x (19.8.15 is deprecated)
          - nx-version: '19.8.14' # 19.x
            node-version: 22
          - nx-version: '19.8.14'
            node-version: 20
          - nx-version: '19.8.14'
            node-version: 18

    name: Backwards Compatibility Test
    uses: ./.github/workflows/smoke-test-nx-workspace.yml
    with:
      nx-version: ${{matrix.nx-version}}
      node-version: ${{matrix.node-version}}
